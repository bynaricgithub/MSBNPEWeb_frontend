import React from "react";
import ReusableTableContent from "../CommonComponent/ReusableTabelContent";

const NoticeBoard = () => {
  return (
    <div>
      <ReusableTableContent type={5} title="Notice Board" />
    </div>
  );
};

export default NoticeBoard;
